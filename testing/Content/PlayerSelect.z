class PlayerSelect : ZilchComponent
{
    [Property]
    var Player1 : Boolean = false;
    var Player2 : Boolean = false;
    var Player3 : Boolean = false;
    var Player4 : Boolean = false;

    function Initialize(init : CogInitializer)
    {
        Zero.Connect(this.Space, Events.LogicUpdate, this.OnLogicUpdate);
    }

    function OnLogicUpdate(event : UpdateEvent)
    {
        if(Zero.Keyboard.KeyIsDown(Keys.NumPad1))
        {





        }
        if(Zero.Keyboard.KeyIsDown(Keys.NumPad2))
        {



        }
        if(Zero.Keyboard.KeyIsDown(Keys.NumPad3))
        {



        }
        if(Zero.Keyboard.KeyIsDown(Keys.NumPad4))
        {


            
        }
    
    }
}